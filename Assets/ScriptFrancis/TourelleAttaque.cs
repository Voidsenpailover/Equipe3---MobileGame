using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TourelleAttaque : MonoBehaviour
{
    // Start is called before the first frame update
    [SerializeField] Tourelle tourelle;
    private void Initialise(Tourelle _tourelle)
    {
        tourelle = _tourelle;
    }

    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
